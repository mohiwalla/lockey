# AGENTS.md

## Development rules

- Use `bun` as the package manager.
- Use `shadcn` as the UI library.
- Use `tailwindcss` as the CSS framework.

## Code conventions

- All identifiers MUST use uppercase ID suffix (e.g., userID, NOT userId) and NEVER mix styles.
